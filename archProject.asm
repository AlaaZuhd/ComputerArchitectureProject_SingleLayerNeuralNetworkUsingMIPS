git colne link from git hub
-------------------------------------
- to push data to github

git add * '''' or specify something"
git commit -m "comment"
git push origin master 

------------------------------------
--- to get data from github
git pull origin

------------------------------------
push an existing repo to github

git init
git add *
git remote add origin https://github.com/AlaaZuhd/newRepo.git
git push -u origin main

------------------------------------
-- files saved, but not added yet' 
git add *
git stash

to get this files again
git stash pop
then i can deal with them us normal
every pop is for one stash before
if i save two file once then i can pop them once else i need more than one op 

----------------------------------- 

